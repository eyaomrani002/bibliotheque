{% extends 'base.html.twig' %}

{% block title %}Modifier Avis{% endblock %}

{% block body %}
<div class="container mx-auto px-6 py-8">
    <div class="max-w-2xl mx-auto bg-white dark:bg-gray-800 rounded-lg shadow p-6">
        <div class="flex items-center justify-between mb-4">
            <h1 class="text-xl font-bold">Modifier l'avis</h1>
            {{ include('avis/_delete_form.html.twig') }}
        </div>

        {{ include('avis/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="mt-4">
            <a href="{{ path('app_avis_index') }}" class="text-sm text-blue-600 hover:underline">Retour à la liste</a>
        </div>
    </div>
</div>
{% endblock %}
