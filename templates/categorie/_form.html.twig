{{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        {# Champ Désignation #}
        <div class="space-y-2">
            {{ form_label(form.designation, null, {'label_attr': {'class': 'block text-sm font-semibold text-gray-700'}}) }}
            {{ form_widget(form.designation, {'attr': {'class': 'w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-all duration-200'}}) }}
            {{ form_errors(form.designation) }}
        </div>

        {# Champ Code #}
        <div class="space-y-2">
            {{ form_label(form.code, null, {'label_attr': {'class': 'block text-sm font-semibold text-gray-700'}}) }}
            {{ form_widget(form.code, {'attr': {'class': 'w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-all duration-200'}}) }}
            {{ form_errors(form.code) }}
        </div>
    </div>

    {# Champ Description #}
    <div class="space-y-2">
        {{ form_label(form.description, null, {'label_attr': {'class': 'block text-sm font-semibold text-gray-700'}}) }}
        {{ form_widget(form.description, {'attr': {'class': 'w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-all duration-200 min-h-[120px]'}}) }}
        {{ form_errors(form.description) }}
    </div>

    {# Bouton Submit #}
    <div class="pt-6">
        <button type="submit" 
                class="w-full md:w-auto px-8 py-3 bg-gradient-to-r from-indigo-500 to-purple-600 hover:from-indigo-600 hover:to-purple-700 text-white font-semibold rounded-xl shadow-lg transform hover:scale-105 transition-all duration-300 flex items-center justify-center space-x-2">
            <i class="fas fa-save"></i>
            <span>{{ button_label|default('Enregistrer') }}</span>
        </button>
    </div>

{{ form_end(form) }}