{% extends 'base.html.twig' %}

{% block title %}Gestion des Catégories{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-8 max-w-7xl">
    {# En-tête avec titre et bouton #}
    <div class="flex justify-between items-center mb-8">
        <div>
            <h1 class="text-3xl font-bold text-gray-800">🏷️ Gestion des Catégories</h1>
            <p class="text-gray-600 mt-2">Organisez vos éléments par catégorie</p>
        </div>
        <a href="{{ path('app_categorie_new') }}" 
           class="bg-gradient-to-r from-green-500 to-teal-600 hover:from-green-600 hover:to-teal-700 text-white px-6 py-3 rounded-xl shadow-lg transition-all duration-300 flex items-center space-x-2 transform hover:scale-105">
            <i class="fas fa-plus-circle"></i>
            <span>Nouvelle Catégorie</span>
        </a>
    </div>

    {# Tableau moderne #}
    <div class="bg-white rounded-2xl shadow-xl overflow-hidden">
        <div class="overflow-x-auto">
            <table class="w-full">
                <thead class="bg-gradient-to-r from-gray-800 to-gray-900">
                    <tr>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-white uppercase tracking-wider">ID</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-white uppercase tracking-wider">Catégorie</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-white uppercase tracking-wider">Code</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-white uppercase tracking-wider">Description</th>
                        <th class="px-6 py-4 text-right text-xs font-semibold text-white uppercase tracking-wider">Actions</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-200">
                    {% for categorie in categories %}
                        <tr class="hover:bg-indigo-50 transition-colors duration-200">
                            <td class="px-6 py-4 whitespace-nowrap">
                                <span class="text-sm font-mono text-gray-500">#{{ categorie.id }}</span>
                            </td>
                            <td class="px-6 py-4">
                                <div class="flex items-center">
                                    <div class="flex-shrink-0 h-10 w-10 bg-gradient-to-br from-blue-400 to-indigo-500 rounded-full flex items-center justify-center text-white font-semibold">
                                        {{ categorie.designation|first|upper }}
                                    </div>
                                    <div class="ml-4">
                                        <div class="text-sm font-semibold text-gray-900">{{ categorie.designation }}</div>
                                        <div class="text-xs text-gray-500">{{ categorie.code }}</div>
                                    </div>
                                </div>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <span class="inline-flex px-3 py-1 text-xs font-semibold rounded-full bg-blue-100 text-blue-800">
                                    {{ categorie.code }}
                                </span>
                            </td>
                            <td class="px-6 py-4">
                                <span class="text-sm text-gray-700">
                                    {{ categorie.description|length > 60 ? categorie.description|slice(0, 60) ~ '...' : categorie.description }}
                                </span>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-right space-x-2">
                                <a href="{{ path('app_categorie_show', {'id': categorie.id}) }}" 
                                   class="inline-flex items-center px-3 py-2 text-sm font-medium rounded-lg text-blue-700 bg-blue-100 hover:bg-blue-200 transition-colors">
                                    <i class="fas fa-eye mr-1"></i> Voir
                                </a>
                                <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}" 
                                   class="inline-flex items-center px-3 py-2 text-sm font-medium rounded-lg text-amber-700 bg-amber-100 hover:bg-amber-200 transition-colors">
                                    <i class="fas fa-edit mr-1"></i> Modifier
                                </a>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="5" class="px-6 py-12 text-center">
                                <i class="fas fa-folder-open text-5xl text-gray-300 mb-4"></i>
                                <p class="text-gray-500 text-lg">Aucune catégorie trouvée</p>
                                <a href="{{ path('app_categorie_new') }}" class="mt-4 inline-block text-indigo-600 hover:text-indigo-800">
                                    Créer votre première catégorie →
                                </a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    {# Footer avec stats #}
    {% if categories|length > 0 %}
    <div class="mt-6 flex justify-between items-center text-sm text-gray-600">
        <span>{{ categories|length }} catégorie(s) au total</span>
        <span>Mis à jour à l'instant</span>
    </div>
    {% endif %}
</div>
{% endblock %}