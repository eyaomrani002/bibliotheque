<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <style>
        body { 
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
            line-height: 1.6; 
            color: #333; 
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            margin: 0; 
            padding: 20px;
        }
        .container { 
            max-width: 700px; 
            margin: 0 auto; 
            background: white; 
            border-radius: 20px; 
            overflow: hidden;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
        }
        .header { 
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%); 
            padding: 30px; 
            text-align: center; 
            color: white;
        }
        .content { 
            padding: 40px 30px; 
        }
        .info-card {
            background: #f8f9fa;
            border-left: 4px solid #667eea;
            border-radius: 10px;
            padding: 20px;
            margin: 20px 0;
        }
        .user-details {
            background: #e8f4fd;
            border-radius: 10px;
            padding: 20px;
            margin: 20px 0;
        }
        .button { 
            display: inline-block; 
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); 
            color: white; 
            text-decoration: none; 
            padding: 12px 25px; 
            border-radius: 50px; 
            font-weight: bold; 
            margin: 10px 5px; 
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }
        .footer { 
            background: #f8f9fa; 
            padding: 20px 30px; 
            text-align: center; 
            color: #6c757d; 
            font-size: 14px;
            border-top: 1px solid #e9ecef;
        }
        .alert {
            background: #fff3cd;
            border: 1px solid #ffeaa7;
            border-radius: 10px;
            padding: 15px;
            margin: 15px 0;
            color: #856404;
        }
        .reset-link {
            background: #f8f9fa;
            border: 1px solid #e9ecef;
            border-radius: 10px;
            padding: 15px;
            margin: 15px 0;
            word-break: break-all;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1 style="margin: 0; font-size: 24px;">🔐 Notification Administrateur</h1>
            <p style="margin: 10px 0 0 0; opacity: 0.9;">Demande de réinitialisation de mot de passe</p>
        </div>
        
        <div class="content">
            <h2 style="color: #667eea; margin-top: 0;">Bonjour Administrateur,</h2>
            
            <p>Un utilisateur a demandé la réinitialisation de son mot de passe sur Biblio-Symfony.</p>
            
            <div class="user-details">
                <h3 style="margin-top: 0; color: #2c3e50;">👤 Informations de l'utilisateur :</h3>
                <p><strong>Email :</strong> {{ user.email }}</p>
                <p><strong>Nom :</strong> {{ user.nom ?? 'Non renseigné' }}</p>
                <p><strong>Prénom :</strong> {{ user.prenom ?? 'Non renseigné' }}</p>
                <p><strong>Date de la demande :</strong> {{ requestedAt|date('d/m/Y à H:i') }}</p>
                <p><strong>Adresse IP :</strong> {{ userIp }}</p>
            </div>

            <div class="info-card">
                <h3 style="color: #2c3e50;">📊 Actions possibles :</h3>
                <p>Vous pouvez :</p>
                <ul>
                    <li>Laisser l'utilisateur réinitialiser son mot de passe via le lien envoyé</li>
                    <li>Générer un nouveau mot de passe temporaire depuis l'administration</li>
                    <li>Contacter l'utilisateur si nécessaire</li>
                    <li>Surveiller cette activité depuis le panneau d'administration</li>
                </ul>
            </div>

            {% if adminLink is defined and adminLink %}
            <div style="margin: 20px 0;">
                <a href="{{ adminLink }}" class="button">🔎 Voir la fiche utilisateur dans l'administration</a>
            </div>
            {% endif %}

            <div class="reset-link">
                <strong>🔗 Lien de réinitialisation :</strong><br>
                <a href="{{ resetUrl }}" style="color: #667eea; text-decoration: none;">
                    {{ resetUrl }}
                </a>
            </div>

            <div class="alert">
                <strong>⚠️ Sécurité :</strong> Cette notification vous permet de suivre les activités de réinitialisation de mot de passe sur votre plateforme. Le lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
            </div>
        </div>
        
        <div class="footer">
            <p>© {{ "now"|date("Y") }} Biblio-Symfony. Tous droits réservés.</p>
            <p>Cet email a été généré automatiquement par le système de sécurité.</p>
        </div>
    </div>
</body>
</html>