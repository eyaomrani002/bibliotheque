<nav class="sticky top-0 z-50 bg-slate-800/90 backdrop-blur-md shadow-xl border-b border-white/10">
  <div class="container-fluid mx-auto px-4">
    <div class="flex items-center justify-between h-16">
      <!-- Brand -->
      <a href="{{ path('app_accueil') }}" class="flex items-center space-x-3 group">
        <div class="w-10 h-10 bg-gradient-to-br from-indigo-500 to-purple-600 rounded-xl flex items-center justify-center text-white text-xl shadow-lg group-hover:scale-110 transition-transform">
          <i class="bi bi-speedometer2"></i>
        </div>
        <div>
          <div class="text-white font-bold text-lg tracking-tight">Admin</div>
          <div class="text-slate-300 text-sm">Bibliothèque</div>
        </div>
      </a>

      <!-- Toggle mobile -->
      <button type="button" class="lg:hidden p-2 rounded-lg bg-white/10 text-white hover:bg-white/20 transition-colors" data-bs-toggle="collapse" data-bs-target="#navbarAdmin">
        <i class="fas fa-bars text-xl"></i>
      </button>

      <!-- Menu -->
      <div class="hidden lg:flex flex-grow items-center justify-between">
        <ul class="flex items-center space-x-2 ml-8">
          <li>
            <a href="{{ path('app_accueil') }}" class="flex items-center px-4 py-2 rounded-lg text-slate-300 hover:text-white hover:bg-white/10 transition-all hover:-translate-y-0.5">
              <i class="bi bi-house mr-2"></i>
              <span>Retour au site</span>
            </a>
          </li>
          <li>
            <a href="{{ path('app_livre_index') }}" class="flex items-center px-4 py-2 rounded-lg text-slate-300 hover:text-white hover:bg-white/10 transition-all hover:-translate-y-0.5">
              <i class="bi bi-journal-text mr-2"></i>
              <span>Livres</span>
            </a>
          </li>
          <li>
            <a href="{{ path('app_auteur_index') }}" class="flex items-center px-4 py-2 rounded-lg text-slate-300 hover:text-white hover:bg-white/10 transition-all hover:-translate-y-0.5">
              <i class="bi bi-pen mr-2"></i>
              <span>Auteurs</span>
            </a>
          </li>
          <li>
            <a href="{{ path('app_categorie_index') }}" class="flex items-center px-4 py-2 rounded-lg text-slate-300 hover:text-white hover:bg-white/10 transition-all hover:-translate-y-0.5">
              <i class="bi bi-tags mr-2"></i>
              <span>Catégories</span>
            </a>
          </li>
          <li>
            <a href="{{ path('app_editeur_index') }}" class="flex items-center px-4 py-2 rounded-lg text-slate-300 hover:text-white hover:bg-white/10 transition-all hover:-translate-y-0.5">
              <i class="bi bi-building mr-2"></i>
              <span>Éditeurs</span>
            </a>
          </li>
        </ul>

        <!-- Actions -->
        <div class="flex items-center space-x-4">
          <a href="{{ path('admin') }}" class="flex items-center px-4 py-2 bg-gradient-to-r from-indigo-500 to-purple-600 text-white rounded-xl font-semibold shadow-lg hover:shadow-xl transform hover:-translate-y-0.5 transition-all">
            <i class="bi bi-grid mr-2"></i>
            <span>Dashboard</span>
          </a>

          <div class="flex items-center space-x-3 px-4 py-2 rounded-xl bg-white/10">
            <div class="w-10 h-10 bg-gradient-to-br from-pink-500 to-rose-500 rounded-full flex items-center justify-center text-white shadow-lg">
              <i class="bi bi-person-circle"></i>
            </div>
            <div>
              <div class="text-white font-medium text-sm">{{ app.user ? app.user.email : 'Admin' }}</div>
              <div class="text-emerald-400 text-xs flex items-center">
                <span class="w-2 h-2 bg-emerald-400 rounded-full mr-1 animate-pulse"></span>
                En ligne
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Menu mobile -->
      <div class="lg:hidden collapse" id="navbarAdmin">
        <div class="py-4 space-y-2 mt-2 border-t border-white/10">
          <a href="{{ path('app_accueil') }}" class="flex items-center px-4 py-3 rounded-lg text-slate-300 hover:text-white hover:bg-white/10 transition-all">
            <i class="bi bi-house mr-3"></i> Retour au site
          </a>
          <a href="{{ path('app_livre_index') }}" class="flex items-center px-4 py-3 rounded-lg text-slate-300 hover:text-white hover:bg-white/10 transition-all">
            <i class="bi bi-journal-text mr-3"></i> Livres
          </a>
          <a href="{{ path('app_auteur_index') }}" class="flex items-center px-4 py-3 rounded-lg text-slate-300 hover:text-white hover:bg-white/10 transition-all">
            <i class="bi bi-pen mr-3"></i> Auteurs
          </a>
          <a href="{{ path('app_categorie_index') }}" class="flex items-center px-4 py-3 rounded-lg text-slate-300 hover:text-white hover:bg-white/10 transition-all">
            <i class="bi bi-tags mr-3"></i> Catégories
          </a>
          <a href="{{ path('app_editeur_index') }}" class="flex items-center px-4 py-3 rounded-lg text-slate-300 hover:text-white hover:bg-white/10 transition-all">
            <i class="bi bi-building mr-3"></i> Éditeurs
          </a>
          <div class="flex items-center justify-between px-4 py-4 mt-4 border-t border-white/10">
            <a href="{{ path('admin') }}" class="flex items-center px-4 py-2 bg-gradient-to-r from-indigo-500 to-purple-600 text-white rounded-xl font-semibold">
              <i class="bi bi-grid mr-2"></i> Dashboard
            </a>
            <div class="text-emerald-400 text-sm">
              <span class="w-2 h-2 bg-emerald-400 rounded-full mr-1 inline-block animate-pulse"></span>
              En ligne
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>