{% extends 'base.html.twig' %}

{% block title %}Email de réinitialisation envoyé - Biblio-Symfony{% endblock %}

{% block body %}
<div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-gray-50 via-white to-indigo-50 py-12 px-4">
    <div class="bg-white rounded-2xl shadow-2xl overflow-hidden w-full max-w-md transform transition-all duration-300">
        {# En-tête #}
        <div class="bg-gradient-to-r from-green-500 to-emerald-600 px-8 py-8 text-center relative overflow-hidden">
            <div class="absolute inset-0 bg-white opacity-10 transform rotate-12"></div>
            <i class="fas fa-paper-plane text-5xl text-white mb-4 relative z-10 drop-shadow-lg"></i>
            <h1 class="text-2xl font-bold text-white relative z-10">Email envoyé !</h1>
            <p class="text-green-100 mt-2 relative z-10">Vérifiez votre boîte de réception</p>
        </div>

        {# Contenu #}
        <div class="p-8">
            <div class="text-center space-y-4">
                <div class="bg-blue-50 border-l-4 border-blue-500 rounded-lg p-4 text-left">
                    <i class="fas fa-info-circle text-blue-500 mr-2"></i>
                    <p class="text-sm text-blue-700">
                        Si un compte correspond à votre email existe, un email vient d'être envoyé contenant un lien pour réinitialiser votre mot de passe.
                    </p>
                </div>

                <div class="bg-yellow-50 border-l-4 border-yellow-500 rounded-lg p-4 text-left">
                    <i class="fas fa-clock text-yellow-500 mr-2"></i>
                    <p class="text-sm text-yellow-700">
                        Ce lien expirera dans <strong>{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}</strong>.
                    </p>
                </div>

                <div class="bg-gray-50 rounded-lg p-4 text-left">
                    <i class="fas fa-shield-alt text-gray-500 mr-2"></i>
                    <p class="text-sm text-gray-600">
                        Si vous ne recevez pas d'email, vérifiez votre dossier spam ou 
                        <a href="{{ path('app_forgot_password_request') }}" class="text-indigo-600 hover:text-indigo-800 font-medium">
                            réessayez
                        </a>.
                    </p>
                </div>
            </div>

            {# Actions #}
            <div class="mt-8 pt-6 border-t border-gray-200 space-y-4">
                <a href="{{ path('app_login') }}" 
                   class="w-full inline-flex items-center justify-center px-6 py-3 bg-gradient-to-r from-indigo-500 to-purple-600 hover:from-indigo-600 hover:to-purple-700 text-white font-semibold rounded-xl shadow-lg transition-all duration-300 transform hover:scale-105">
                    <i class="fas fa-sign-in-alt mr-2"></i>
                    Retour à la connexion
                </a>
                
                <a href="{{ path('app_accueil') }}" 
                   class="w-full inline-flex items-center justify-center px-6 py-3 border border-gray-300 text-gray-700 hover:bg-gray-50 font-medium rounded-xl transition-all duration-200">
                    <i class="fas fa-home mr-2"></i>
                    Retour à l'accueil
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}