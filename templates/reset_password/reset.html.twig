{% extends 'base.html.twig' %}

{% block title %}Nouveau mot de passe - Biblio-Symfony{% endblock %}

{% block body %}
<div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-gray-50 via-white to-indigo-50 py-12 px-4">
    <div class="bg-white rounded-2xl shadow-2xl overflow-hidden w-full max-w-md transform transition-all duration-300">
        {# En-tête #}
        <div class="bg-gradient-to-r from-green-500 to-emerald-600 px-8 py-8 text-center relative overflow-hidden">
            <div class="absolute inset-0 bg-white opacity-10 transform rotate-12"></div>
            <i class="fas fa-lock-open text-5xl text-white mb-4 relative z-10 drop-shadow-lg"></i>
            <h1 class="text-2xl font-bold text-white relative z-10">Nouveau mot de passe</h1>
            <p class="text-green-100 mt-2 relative z-10">Définissez votre nouveau mot de passe</p>
        </div>

        {# Formulaire #}
        <div class="p-8">
            {{ form_start(resetForm, {'attr': {'class': 'space-y-6'}}) }}
                
                {# Adresse email du compte (readonly) #}
                <div class="space-y-2">
                    <label class="block text-sm font-semibold text-gray-700 mb-2">Adresse e-mail du compte</label>
                    <div class="relative">
                        <input type="text" value="{{ userEmail }}" disabled class="w-full pl-12 pr-4 py-3 border border-gray-300 rounded-xl bg-gray-100 text-gray-700" />
                        <input type="hidden" name="email" value="{{ userEmail }}" />
                        <i class="fas fa-envelope absolute left-4 top-1/2 -translate-y-1/2 text-gray-400"></i>
                    </div>
                </div>

                {# Champ Mot de passe #}
                <div class="space-y-2">
                    {{ form_label(resetForm.newPassword.first, 'Nouveau mot de passe', {
                        'label_attr': {
                            'class': 'block text-sm font-semibold text-gray-700 mb-2 flex items-center'
                        }
                    }) }}
                    <div class="relative">
                        {{ form_widget(resetForm.newPassword.first, {
                            'attr': {
                                'class': 'w-full pl-12 pr-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-all duration-200 bg-gray-50 hover:bg-white',
                                'placeholder': 'Votre nouveau mot de passe'
                            }
                        }) }}
                        <i class="fas fa-lock absolute left-4 top-1/2 -translate-y-1/2 text-gray-400"></i>
                    </div>
                </div>

                {# Champ Confirmation #}
                <div class="space-y-2">
                    {{ form_label(resetForm.newPassword.second, 'Confirmer le mot de passe', {
                        'label_attr': {
                            'class': 'block text-sm font-semibold text-gray-700 mb-2 flex items-center'
                        }
                    }) }}
                    <div class="relative">
                        {{ form_widget(resetForm.newPassword.second, {
                            'attr': {
                                'class': 'w-full pl-12 pr-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-all duration-200 bg-gray-50 hover:bg-white',
                                'placeholder': 'Confirmez votre mot de passe'
                            }
                        }) }}
                        <i class="fas fa-lock absolute left-4 top-1/2 -translate-y-1/2 text-gray-400"></i>
                    </div>
                </div>

                {{ form_errors(resetForm.newPassword) }}

                {# Indications de sécurité #}
                <div class="bg-green-50 border-l-4 border-green-500 rounded-lg p-4">
                    <p class="text-sm text-green-700 flex items-start">
                        <i class="fas fa-shield-alt text-green-500 mr-2 mt-0.5 flex-shrink-0"></i>
                        Choisissez un mot de passe sécurisé avec au moins 8 caractères, incluant des majuscules, minuscules et chiffres.
                    </p>
                </div>

                {# Bouton #}
                <button type="submit" 
                        class="w-full px-8 py-3 bg-gradient-to-r from-green-500 to-emerald-600 hover:from-green-600 hover:to-emerald-700 text-white font-semibold rounded-xl shadow-lg transform transition-all duration-300 flex items-center justify-center space-x-2 hover:scale-105 active:scale-95">
                    <i class="fas fa-save"></i>
                    <span>Enregistrer le nouveau mot de passe</span>
                </button>

            {{ form_end(resetForm) }}

            {# Liens #}
            <div class="mt-8 pt-6 border-t border-gray-200 space-y-4 text-center">
                <a href="{{ path('app_login') }}" 
                   class="inline-flex items-center text-sm text-indigo-600 hover:text-indigo-800 font-medium transition-all duration-200 group">
                    <i class="fas fa-arrow-left mr-2"></i>
                    Retour à la connexion
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}