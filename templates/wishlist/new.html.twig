{% extends 'base.html.twig' %}

{% block title %}Nouvelle wishlist{% endblock %}

{% block body %}
<div class="max-w-3xl mx-auto py-8">
    <div class="bg-white/80 dark:bg-gray-900/70 rounded-2xl shadow-lg p-6 backdrop-blur">
        <h1 class="text-2xl font-bold mb-4 text-gray-900 dark:text-white">Créer une wishlist</h1>

        {{ include('wishlist/_form.html.twig') }}

        <div class="mt-4">
            <a href="{{ path('app_wishlist_index') }}" class="text-sm text-blue-600 hover:underline">Retour à la liste</a>
        </div>
    </div>
</div>
{% endblock %}
